{
  "name": "ine-product-price-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "INE Product Price Tracker — reliable scraper + dashboard (JavaScript)",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "lint": "npm --prefix server run lint & npm --prefix client run lint",
    "test": "npm --prefix server test",
    "scrape:headed": "npm --prefix server run scrape:headed"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
